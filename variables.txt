// Variables

A variable is a container.
Variable acts like the value it contains, like algebra. 1 + x = 3. 1 + const = 3.

var = can be used globally
let/const = can only be used within the same block
let = can be changed
const = cannot be changed once made

const = constant, can't change
let = can change
let <variablename> = "";
const <variablename> = "";
<variable name> = <variable name> <operator> <value>;
one = one + 1;

concatenation = "some" + "thing"

​let one = 1'
one + 1;

console.log(`Woof`); // Print in console
// window.alert(`This is an alert`); // Popup Window

document.getElementById("h1titles").textContent = "Javascript Lessons"; // Populate the h1titles id
document.getElementById("paragraphs").textContent = "This is pretty cool."; // Populate the h1titles id

let x; // create container "x"
x = 100 // the container is 100.
console.log(x); // prints 100 in the console

let age = 25;
console.log(age);
console.log(`You are ${age} years old.`);
console.log(typeof age)

let price = 10.99
console.log(price);
console.log(`The price is $${price}.`);
console.log(typeof price) // numbers can use math

let firstName = "Siren";
console.log(`Your first name is ${firstName}.`)
console.log(typeof firstName)

let favouriteFood = "Pizza";
console.log(`Your favourite food is ${favouriteFood}.`)
console.log(typeof favouriteFood) // strings cannot use math

let online = true; // Boolean (true/false)
console.log(`${firstName} is online: ${online}`);
console.log(typeof online); // can use to show or hide items
