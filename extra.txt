// Extra //

Put Defer in the start to delay the script.

<head>
<script defer src="main.js"></script>
</head>

DOM = Document Object Model - it means page tree, like <html> <head> <body> etc.

var = can be used globally
let/const = can only be used within the same block
let = can be changed
const = cannot be changed once made
